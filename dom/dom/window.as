package dom
{
	import dom.domobjects.Window;
	
	/**
	 * The globally accessible window object provides access to global functions and
	 * objects which are part of the browser.
	 * 
	 * @see dom.domobjects.Window
	 */
	public var window:Window;
}